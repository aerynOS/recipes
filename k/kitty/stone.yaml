# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : kitty
version     : "0.49.1"
release     : 20
homepage    : https://sw.kovidgoyal.net/kitty/
upstreams   :
    - https://github.com/kovidgoyal/kitty/releases/download/v0.49.1/kitty-0.49.1.tar.xz : c134b6268e06871accf787ef8f6c0d0b8a1c327f78e03fa93c9360db2270b532
summary     : Cross-platform, fast, feature-rich, GPU based terminal
description : |
    kitty the fast, feature-rich, GPU based terminal emulator
license     : GPL-3.0-or-later
networking  : true
builddeps   :
    - binary(python3)
    - binary(slangc)
    - binary(sphinx-build)
    - binary(tic)
    - pkgconfig(dbus-1)
    - pkgconfig(gl)
    - pkgconfig(harfbuzz)
    - pkgconfig(lcms2)
    - pkgconfig(libcrypto)
    - pkgconfig(libxxhash)
    - pkgconfig(python3)
    - pkgconfig(simde)
    - pkgconfig(wayland-protocols)
    - pkgconfig(wayland-scanner)
    - pkgconfig(xcursor)
    - pkgconfig(xi)
    - pkgconfig(xinerama)
    - pkgconfig(xkbcommon)
    - pkgconfig(xkbcommon-x11)
    - pkgconfig(xrandr)
    - python(sphinx-copybutton)
    - python(sphinx-inline-tabs)
    - font-symbols-nerd
setup       : |
    %go_mod
build       : |
    python3 setup.py linux-package \
        --skip-building-kitten \
        --update-check-interval=0 \
        --verbose

    go build -o _build/bin/kitten ./tools/cmd
install     : |
    %install_exe _build/bin/kitten -t %(installroot)%(bindir)

    cp -r linux-package/. %(installroot)%(prefix)
    
    _build/bin/kitten __complete__ setup bash | %install_file /dev/stdin %(installroot)%(bashcompletionsdir)/kitty
    _build/bin/kitten __complete__ setup fish | %install_file /dev/stdin %(installroot)%(fishcompletionsdir)/kitty.fish
    _build/bin/kitten __complete__ setup zsh  | %install_file /dev/stdin %(installroot)%(zshcompletionsdir)/_kitty

    %install_file LICENSE -t %(installroot)%(datadir)/licenses/%(name)/
packages     :
    - "%(name)":
        rundeps:
            - "terminfo-%(name)"
            - binary(slangc)

    - "terminfo-%(name)":
        summary: Terminfo for %(name)
        description: Terminfo for %(name), split out so that it can be used from ncurses
        paths:
            - /usr/share/terminfo/

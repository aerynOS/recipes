# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : krdc
version     : "26.08.1"
release     : 20
upstreams   :
    - https://download.kde.org/stable/release-service/26.08.1/src/krdc-26.08.1.tar.xz : 291ce5f05fc4e67ed659eec9aa8c9f1f06bf1b13a5180375e68d2402341a0ed0
homepage    : https://apps.kde.org/krdc/
license     :
    - GFDL-1.2-or-later
    - GPL-2.0-or-later
    - LGPL-2.1-or-later
summary     : KDE's remote desktop client
description : |
    KRDC is a client application that allows you to view or even control the desktop session on another machine that is running a compatible server. VNC and RDP are supported.
builddeps   :
    # Temporary, boulder doesn't handle build ordering resolution correctly when builddeps are added by macros, so add ECM manually
    - cmake(ECM)
    - cmake(FreeRDP)
    - cmake(KF6Bookmarks)
    - cmake(KF6Completion)
    - cmake(KF6Config)
    - cmake(KF6Crash)
    - cmake(KF6DNSSD)
    - cmake(KF6DocTools)
    - cmake(KF6GuiAddons)
    - cmake(KF6I18n)
    - cmake(KF6IconThemes)
    - cmake(KF6KCMUtils)
    - cmake(KF6KIO)
    - cmake(KF6Notifications)
    - cmake(KF6NotifyConfig)
    - cmake(KF6StatusNotifierItem)
    - cmake(KF6WidgetsAddons)
    - cmake(KF6WindowSystem)
    - cmake(KF6XmlGui)
    - cmake(Qt6CorePrivate)
    - cmake(Qt6Keychain)
    - cmake(Qt6WaylandClient)
    - pkgconfig(libssh)
    - pkgconfig(libvncclient)
setup       : |
    %cmake_kf6
build       : |
    %cmake_build
install     : |
    %cmake_install
packages    :
    - "%(name)":
        rundeps:
            - qt6-wayland

    - "%(name)-libs":
        summary: Runtime libraries for %(name)
        description: Runtime libraries for %(name)
        paths:
            - /usr/lib/libkrdccore.so.*
            - /usr/lib/qt6/plugins/krdc/

    - "%(name)-docs":
        paths:
            - /usr/share/doc

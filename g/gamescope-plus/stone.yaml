# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : gamescope-plus
version     : "3.16.22.plus1"
release     : 8
homepage    : https://github.com/OpenGamingCollective/gamescope
upstreams   :
    - git|https://github.com/OpenGamingCollective/gamescope.git : 402bfb81bc25943cac9061eb022fe229c5414f5e
    - git|https://github.com/misyltoad/GamescopeShaders.git : 64d7a0570e15dc5c9aeef084278b9eb4cc1abb25
summary     : SteamOS session compositing window manager with added patches
description : |
    In an embedded session usecase, gamescope does the same thing as steamcompmgr, but with less extra copies and latency. Added with patches by ChimeraOS team.
license     :
    - BSD-2-Clause
    - GPL-3.0-only
networking  : true
builddeps   :
    - binary(glslangValidator)
    - pkgconfig(egl)
    - pkgconfig(gbm)
    - pkgconfig(glesv2)
    - pkgconfig(hwdata)
    - pkgconfig(jsoncpp)
    - pkgconfig(lcms2)
    - pkgconfig(libcap)
    - pkgconfig(libdecor-0)
    - pkgconfig(libdisplay-info)
    - pkgconfig(libdrm)
    - pkgconfig(libeis-1.0)
    - pkgconfig(libinput)
    - pkgconfig(libpipewire-0.3)
    - pkgconfig(libseat)
    - pkgconfig(libudev)
    - pkgconfig(luajit)
    - pkgconfig(pixman-1)
    - pkgconfig(sdl2)
    - pkgconfig(vulkan)
    - pkgconfig(wayland-client)
    - pkgconfig(wayland-protocols)
    - pkgconfig(x11)
    - pkgconfig(xcb-errors)
    - pkgconfig(xcb-ewmh)
    - pkgconfig(xcomposite)
    - pkgconfig(xcursor)
    - pkgconfig(xdamage)
    - pkgconfig(xkbcommon)
    - pkgconfig(xmu)
    - pkgconfig(xres)
    - pkgconfig(xtst)
    - pkgconfig(xwayland)
    - pkgconfig(xxf86vm)
    # TODO: Use system glm
rundeps     :
    - binary(Xwayland)
setup       : |
    %meson \
        --wrap-mode=default \
        -Dforce_fallback_for="libliftoff,stb,vkroots,wlroots"
build       : |
    %meson_build
install     : |
    %install_dir %(installroot)/usr/share/gamescope/reshade
    cp -rv %(workdir)/../GamescopeShaders.git/* %(installroot)/usr/share/gamescope/reshade/
    %meson_install --skip-subprojects

    # TODO: Add capabilities
packages    :
    - "%(name)":
        paths:
            - /usr/lib/*.so

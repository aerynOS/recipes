diff --git a/packages/localsend_isolates/rust_builder/cargokit/build_tool/lib/src/artifacts_provider.dart b/packages/localsend_isolates/rust_builder/cargokit/build_tool/lib/src/artifacts_provider.dart
index e608cec..316a036 100644
--- a/packages/localsend_isolates/rust_builder/cargokit/build_tool/lib/src/artifacts_provider.dart
+++ b/packages/localsend_isolates/rust_builder/cargokit/build_tool/lib/src/artifacts_provider.dart
@@ -12,7 +12,6 @@ import 'builder.dart';
 import 'crate_hash.dart';
 import 'options.dart';
 import 'precompile_binaries.dart';
-import 'rustup.dart';
 import 'target.dart';
 
 class Artifact {
@@ -63,10 +62,8 @@ class ArtifactProvider {
       return result;
     }
 
-    final rustup = Rustup();
     for (final target in targets) {
       final builder = RustBuilder(target: target, environment: environment);
-      builder.prepare(rustup);
       _log.info('Building ${environment.crateInfo.packageName} for $target');
       final targetDir = await builder.build();
       // For local build accept both static and dynamic libraries.
diff --git a/packages/localsend_isolates/rust_builder/cargokit/build_tool/lib/src/builder.dart b/packages/localsend_isolates/rust_builder/cargokit/build_tool/lib/src/builder.dart
index da6cb00..3030b75 100644
--- a/packages/localsend_isolates/rust_builder/cargokit/build_tool/lib/src/builder.dart
+++ b/packages/localsend_isolates/rust_builder/cargokit/build_tool/lib/src/builder.dart
@@ -142,11 +142,8 @@ class RustBuilder {
     final extraArgs = _buildOptions?.flags ?? [];
     final manifestPath = path.join(environment.manifestDir, 'Cargo.toml');
     runCommand(
-      'rustup',
+      'cargo',
       [
-        'run',
-        _getToolchainVersion(environment.manifestDir, _toolchain),
-        'cargo',
         'build',
         ...extraArgs,
         '--manifest-path',
diff --git a/packages/localsend_isolates/rust_builder/cargokit/cmake/cargokit.cmake b/packages/localsend_isolates/rust_builder/cargokit/cmake/cargokit.cmake
index ddd05df..5f7b0a8 100644
--- a/packages/localsend_isolates/rust_builder/cargokit/cmake/cargokit.cmake
+++ b/packages/localsend_isolates/rust_builder/cargokit/cmake/cargokit.cmake
@@ -25,7 +25,7 @@ function(apply_cargokit target manifest_dir lib_name any_symbol_name)
         set(CARGOKIT_OUTPUT_DIR "${CMAKE_CURRENT_BINARY_DIR}")
         set(OUTPUT_LIB "${CMAKE_CURRENT_BINARY_DIR}/${CARGOKIT_LIB_FULL_NAME}")
     endif()
-    set(CARGOKIT_TEMP_DIR "${CMAKE_CURRENT_BINARY_DIR}/cargokit_build")
+    set(CARGOKIT_TEMP_DIR "$ENV{CARGOKIT_TARGET_DIR}")
 
     if (FLUTTER_TARGET_PLATFORM)
         set(CARGOKIT_TARGET_PLATFORM "${FLUTTER_TARGET_PLATFORM}")

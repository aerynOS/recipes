# SPDX-FileCopyrightText: 2026 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : crun
version     : "1.30.1"
release     : 7
homepage    : https://github.com/containers/crun
upstreams   :
    - https://github.com/containers/crun/releases/download/1.30.1/crun-1.30.1.tar.zst : d62b89a82520a553edf63fd3ae11275be6f16ec86ed90d8ef7b113d06b239fa2
summary     : A fast and low-memory footprint OCI Container Runtime fully written in C
description : |
    A fast and lightweight fully featured OCI runtime and C library for running containers
license     :
    - GPL-2.0-or-later
    - LGPL-2.1-or-later
builddeps   :
    - binary(go-md2man)
    - binary(gperf)
    - binary(python3)
    - pkgconfig(json-c)
    - pkgconfig(libblake3)
    - pkgconfig(libcap)
    - pkgconfig(libseccomp)
    - pkgconfig(python3)
    - pkgconfig(systemd)
    # criu
setup       : |
    # python_crun: native module so users can import crun from Python scripts
    %configure \
        --disable-static \
        --enable-shared \
        --with-python-bindings
build       : |
    %make
install     : |
    %make_install
packages    :
    - "python-%(name)":
        summary: Python bindings for %(name)
        description: Python bindings for %(name)
        rundeps:
            - "%(name)"
        paths:
            - /usr/lib/python3*/site-packages/
